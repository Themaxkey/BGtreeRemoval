---
import { site } from '../../site.config';
---
<div class="grid">
  {site.towns.map(t => (
    <a class="card" href={`/${t.slug}/`}>
      <strong>{t.name}, {site.location.stateAbbr}</strong>
      <span>Tree removal and tree services in {t.name}.</span>
    </a>
  ))}
</div>
